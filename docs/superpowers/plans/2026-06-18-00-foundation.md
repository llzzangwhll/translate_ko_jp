# 00 Foundation 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모든 플로우가 공유하는 core 타입·엔티티·Service 인터페이스·DI 골격을 만들어 병렬 작업의 토대를 확정한다.

**Architecture:** 순수 Dart 값 타입(Result/Failure/Language)과 도메인 엔티티를 TDD로 만들고, 동작 없는 Service 인터페이스와 빈 DI 바인딩/라우트 골격을 추가한다. 이후 모든 플로우는 이 계약에 기대어 Fake로 독립 구현한다.

**Tech Stack:** Flutter, Dart 3 (sealed classes), GetX, flutter_test

> 모든 시그니처는 `2026-06-18-00-INDEX.md`의 "공유 계약"과 일치해야 한다.

---

### Task 1: core 실패/결과 타입

**Files:**
- Create: `lib/core/failure.dart`
- Create: `lib/core/result.dart`
- Test: `test/core/result_test.dart`

- [ ] **Step 1: 실패 타입 작성**

`lib/core/failure.dart`:
```dart
sealed class Failure {
  final String message;
  const Failure(this.message);
}

class PermissionFailure extends Failure { const PermissionFailure(super.message); }
class NetworkFailure extends Failure { const NetworkFailure(super.message); }
class ModelFailure extends Failure { const ModelFailure(super.message); }
class InferenceFailure extends Failure { const InferenceFailure(super.message); }
class StorageFailure extends Failure { const StorageFailure(super.message); }
```

- [ ] **Step 2: 결과 타입 작성**

`lib/core/result.dart`:
```dart
import 'failure.dart';

sealed class Result<T> {
  const Result();

  bool get isOk => this is Ok<T>;
  T? get valueOrNull => this is Ok<T> ? (this as Ok<T>).value : null;
  Failure? get failureOrNull => this is Err<T> ? (this as Err<T>).failure : null;
}

class Ok<T> extends Result<T> {
  final T value;
  const Ok(this.value);
}

class Err<T> extends Result<T> {
  final Failure failure;
  const Err(this.failure);
}
```

- [ ] **Step 3: 실패 테스트 작성**

`test/core/result_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:translate_ko_jp/core/result.dart';
import 'package:translate_ko_jp/core/failure.dart';

void main() {
  test('Ok exposes value and isOk', () {
    const Result<int> r = Ok(42);
    expect(r.isOk, isTrue);
    expect(r.valueOrNull, 42);
    expect(r.failureOrNull, isNull);
  });

  test('Err exposes failure and not ok', () {
    const Result<int> r = Err(NetworkFailure('boom'));
    expect(r.isOk, isFalse);
    expect(r.valueOrNull, isNull);
    expect(r.failureOrNull, isA<NetworkFailure>());
    expect(r.failureOrNull!.message, 'boom');
  });
}
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `flutter test test/core/result_test.dart`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/core/failure.dart lib/core/result.dart test/core/result_test.dart
git commit -m "feat: add core Result and Failure types"
```

---

### Task 2: Language enum

**Files:**
- Create: `lib/core/language.dart`
- Test: `test/core/language_test.dart`

- [ ] **Step 1: 테스트 작성 (실패 먼저)**

`test/core/language_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:translate_ko_jp/core/language.dart';

void main() {
  test('ko maps to correct codes', () {
    expect(Language.ko.sttLocale, 'ko_KR');
    expect(Language.ko.ttsCode, 'ko-KR');
    expect(Language.ko.nativeLabel, '한국어');
    expect(Language.ko.promptLabel, 'Korean');
    expect(Language.ko.opposite, Language.ja);
  });

  test('ja maps to correct codes', () {
    expect(Language.ja.sttLocale, 'ja_JP');
    expect(Language.ja.ttsCode, 'ja-JP');
    expect(Language.ja.promptLabel, 'Japanese');
    expect(Language.ja.opposite, Language.ko);
  });
}
```

- [ ] **Step 2: 실행하여 실패 확인**

Run: `flutter test test/core/language_test.dart`
Expected: FAIL (`Language` 미정의 / 컴파일 에러)

- [ ] **Step 3: 구현**

`lib/core/language.dart`:
```dart
enum Language {
  ko,
  ja;

  String get sttLocale => this == Language.ko ? 'ko_KR' : 'ja_JP';
  String get ttsCode => this == Language.ko ? 'ko-KR' : 'ja-JP';
  String get nativeLabel => this == Language.ko ? '한국어' : '日本語';
  String get promptLabel => this == Language.ko ? 'Korean' : 'Japanese';
  Language get opposite => this == Language.ko ? Language.ja : Language.ko;
}
```

- [ ] **Step 4: 실행하여 통과 확인**

Run: `flutter test test/core/language_test.dart`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/core/language.dart test/core/language_test.dart
git commit -m "feat: add Language enum with locale/tts/prompt mappings"
```

---

### Task 3: 도메인 엔티티

**Files:**
- Create: `lib/domain/entities/language_direction.dart`
- Create: `lib/domain/entities/translation_result.dart`
- Create: `lib/domain/entities/model_status.dart`
- Test: `test/domain/entities_test.dart`

- [ ] **Step 1: 테스트 작성 (실패 먼저)**

`test/domain/entities_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:translate_ko_jp/core/language.dart';
import 'package:translate_ko_jp/domain/entities/language_direction.dart';
import 'package:translate_ko_jp/domain/entities/translation_result.dart';

void main() {
  test('koToJa factory and reversed', () {
    final d = LanguageDirection.koToJa();
    expect(d.from, Language.ko);
    expect(d.to, Language.ja);
    expect(d.isKoToJa, isTrue);

    final r = d.reversed;
    expect(r.from, Language.ja);
    expect(r.to, Language.ko);
    expect(r.isKoToJa, isFalse);
  });

  test('LanguageDirection equality', () {
    expect(LanguageDirection.koToJa(), LanguageDirection.koToJa());
    expect(LanguageDirection.koToJa() == LanguageDirection.jaToKo(), isFalse);
  });

  test('TranslationResult copyWith sets id', () {
    final t = TranslationResult(
      sourceText: '안녕',
      translatedText: 'こんにちは',
      direction: LanguageDirection.koToJa(),
      createdAt: DateTime(2026, 1, 1),
    );
    expect(t.id, isNull);
    expect(t.copyWith(id: 5).id, 5);
    expect(t.copyWith(id: 5).sourceText, '안녕');
  });
}
```

- [ ] **Step 2: 실행하여 실패 확인**

Run: `flutter test test/domain/entities_test.dart`
Expected: FAIL (엔티티 미정의)

- [ ] **Step 3: 구현**

`lib/domain/entities/language_direction.dart`:
```dart
import '../../core/language.dart';

class LanguageDirection {
  final Language from;
  final Language to;
  const LanguageDirection({required this.from, required this.to});

  factory LanguageDirection.koToJa() =>
      const LanguageDirection(from: Language.ko, to: Language.ja);
  factory LanguageDirection.jaToKo() =>
      const LanguageDirection(from: Language.ja, to: Language.ko);

  LanguageDirection get reversed => LanguageDirection(from: to, to: from);
  bool get isKoToJa => from == Language.ko;

  @override
  bool operator ==(Object other) =>
      other is LanguageDirection && other.from == from && other.to == to;
  @override
  int get hashCode => Object.hash(from, to);
}
```

`lib/domain/entities/translation_result.dart`:
```dart
import 'language_direction.dart';

class TranslationResult {
  final int? id;
  final String sourceText;
  final String translatedText;
  final LanguageDirection direction;
  final DateTime createdAt;

  const TranslationResult({
    this.id,
    required this.sourceText,
    required this.translatedText,
    required this.direction,
    required this.createdAt,
  });

  TranslationResult copyWith({int? id}) => TranslationResult(
        id: id ?? this.id,
        sourceText: sourceText,
        translatedText: translatedText,
        direction: direction,
        createdAt: createdAt,
      );
}
```

`lib/domain/entities/model_status.dart`:
```dart
enum ModelStatus { notDownloaded, downloading, downloaded, loaded, error }
```

- [ ] **Step 4: 실행하여 통과 확인**

Run: `flutter test test/domain/entities_test.dart`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/domain/entities/ test/domain/entities_test.dart
git commit -m "feat: add domain entities (LanguageDirection, TranslationResult, ModelStatus)"
```

---

### Task 4: Service 인터페이스 (동작 없음)

**Files:**
- Create: `lib/data/services/speech_service.dart`
- Create: `lib/data/services/tts_service.dart`
- Create: `lib/data/services/inference_service.dart`
- Create: `lib/data/services/model_download_service.dart`
- Create: `lib/data/services/history_store.dart`

- [ ] **Step 1: 인터페이스 파일 작성**

`lib/data/services/speech_service.dart`:
```dart
import '../../core/language.dart';

class SpeechResult {
  final String text;
  final bool isFinal;
  const SpeechResult(this.text, this.isFinal);
}

abstract interface class SpeechService {
  Future<bool> initialize();
  bool get isAvailable;
  Future<void> listen({
    required Language language,
    required void Function(SpeechResult result) onResult,
  });
  Future<void> stop();
  Future<void> cancel();
}
```

`lib/data/services/tts_service.dart`:
```dart
import '../../core/language.dart';

abstract interface class TtsService {
  Future<void> initialize();
  Future<void> speak({required String text, required Language language});
  Future<void> stop();
}
```

`lib/data/services/inference_service.dart`:
```dart
import '../../domain/entities/language_direction.dart';

abstract interface class InferenceService {
  Future<bool> modelExists();
  Future<bool> isLoaded();
  Future<void> load({String? modelPath});
  Future<String> translate({
    required String text,
    required LanguageDirection direction,
  });
  Future<void> dispose();
}
```

`lib/data/services/model_download_service.dart`:
```dart
class DownloadProgress {
  final int received;
  final int total;
  final bool done;
  const DownloadProgress({
    required this.received,
    required this.total,
    required this.done,
  });
  double get fraction => total > 0 ? received / total : 0.0;
}

abstract interface class ModelDownloadService {
  Stream<DownloadProgress> download({
    required String url,
    required String destPath,
    String? sha256,
    Map<String, String>? headers,
  });
  void cancel();
}
```

`lib/data/services/history_store.dart`:
```dart
import '../../domain/entities/translation_result.dart';

abstract interface class HistoryStore {
  Future<int> insert(TranslationResult result);
  Future<List<TranslationResult>> getAll();
  Future<void> delete(int id);
  Future<void> clear();
}
```

- [ ] **Step 2: 정적 분석으로 검증**

Run: `flutter analyze lib/data/services lib/core lib/domain`
Expected: `No issues found!`

- [ ] **Step 3: 커밋**

```bash
git add lib/data/services/
git commit -m "feat: add service interface contracts"
```

---

### Task 5: DI 바인딩 & 라우트 골격

**Files:**
- Create: `lib/app/routes.dart`
- Create: `lib/app/bindings.dart`

- [ ] **Step 1: 라우트 상수 작성**

`lib/app/routes.dart`:
```dart
abstract final class Routes {
  static const setup = '/setup';
  static const translation = '/translate';
  static const history = '/history';
}
```

- [ ] **Step 2: 빈 바인딩 골격 작성**

`lib/app/bindings.dart`:
```dart
import 'package:get/get.dart';

/// Each flow contributes its own registration function and the integration
/// step (plan 05) wires them together here.
class AppBinding extends Bindings {
  @override
  void dependencies() {
    // Flow registrations are added during integration (plan 05):
    // registerTranslationDeps();
    // registerModelDeps();
    // registerHistoryDeps();
  }
}
```

- [ ] **Step 3: 정적 분석으로 검증**

Run: `flutter analyze lib/app`
Expected: `No issues found!`

- [ ] **Step 4: 커밋**

```bash
git add lib/app/routes.dart lib/app/bindings.dart
git commit -m "feat: add routing constants and DI binding skeleton"
```

---

## 완료 기준

- [ ] `flutter test test/core test/domain` 전부 통과
- [ ] `flutter analyze` 신규 파일에서 이슈 없음
- [ ] 공유 계약(INDEX)과 시그니처 일치
- [ ] 이후 01~04 플로우가 이 인터페이스에 기대어 병렬 착수 가능
